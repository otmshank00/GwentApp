@using GwentApp.Models
@model List<Card>  

@{
    ViewBag.Title = "CreateDeck";
}

<h2>CreateDeck</h2>
<ul>
    @foreach (Card card in Model)
    {
        <li>@card.name | @card.power</li>
    }
</ul>

